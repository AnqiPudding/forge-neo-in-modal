# Modal Model Downloader

Small Forge/WebUI extension for downloading a direct URL into the mounted Modal
models folder. It supports a custom subfolder under `models`, an optional
filename override, and an optional bearer token for gated URLs.
